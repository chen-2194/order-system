server:
  port: 8080

spring:
  datasource:
   # url: jdbc:postgresql://localhost:5432/order_system
    url: jdbc:postgresql://order-system-db.c1caey2yk2jx.ap-southeast-2.rds.amazonaws.com:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update          # 自动创建/更新表结构
    show-sql: true              # 控制台打印 SQL
    properties:
      hibernate:
        format_sql: true        # 格式化 SQL
    #    dialect: org.hibernate.dialect.PostgreSQLDialect